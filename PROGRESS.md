# Progress log

One line per finished script: what was produced, row counts, anything surprising.

- **01_clean.py** → `data/interim/01_products.parquet` (131,930×30), `01_notes_long.parquet` (1,098,223 rows), `01_accords_long.parquet` (1,000,570 rows), `01_ai_summary_long.parquet` (210,521 rows). `in_population` (people≥50, ≥1 note, year 1990-2025) = 32,499 products. Surprising: the schema's "vote aggregates go null together" claim doesn't hold in this dump — `people` goes null independently of relation/seasons/daypart/community_gender, which stay present as real all-zero dicts; added `has_votes` to null those groups out only when their own dict sums to zero, checked per-group, not inferred from `people`.
- **02_normalise_notes.py** → `outputs/note_normalisation.csv` (2,029 raw tokens → 492 canonical notes after review). Surprising: naive word-containment auto-merge is dangerous — it transitively chained "sandalwood" into a 450-token/30,782-product blob (apple, rose, tea, chocolate...) via hub words like "water"/"oil"; rebuilt around edit-distance-gated fuzzy matching + a "remainder isn't itself a real word" guard, plus a hardcoded manual override list (ambergris/amberwood un-merged from amber, clover/straw/mango un-merged, virginia cedar→cedar and mandarin orange→mandarin promoted) for full reproducibility.
- **03_taxonomy.py** → `outputs/taxonomy_map.csv` (492 notes × 15 clusters), `outputs/taxonomy_validation.csv` (top-5 accords by lift/cluster, accords with population count <100 excluded as unstable), `data/interim/03_product_family.parquet` (103,492 rows, fractional membership). Silhouette: k=12→0.037, k=15→0.031 (used), k=18→0.044. Surprising: cluster 4 ("soapy") is a real, tightly-separated ~76-note cluster in SVD space but it's essentially the "ubiquitous backbone notes" bucket (musk, bergamot, sandalwood, vanilla, jasmine, amber, patchouli, cedar, rose...) — it holds ~59% of all launches, which dominates cluster 4's supply share downstream. Worth a look before renaming clusters.
- **04_demand.py** → `data/interim/04_family_demand.parquet` (15 families × raw/demeaned approval, desire, adoption, value). 0 nulls on all four signals within population (people≥50 guarantees relation/rating always present). 93.4% of products got brand-demeaned (1,160/2,261 population brands have ≥5 products).
- **05_sentiment.py** → `data/interim/05_family_sentiment.parquet`. Subpopulation (in_population AND has_ai_summary) = 11,579 products (35.6% of population), skews toward more-voted/slightly-newer/slightly-higher-rated products (mean people 1,332 vs 577; mean year 2016.9 vs 2015.8; mean rating 4.00 vs 3.96) — expected, ai_summary is concentrated on popular products.
- **05b_seasonality.py** → `data/interim/05b_family_season.parquet`. All 32,499 population products had season votes (has_votes always true in-population). Oud/whiskey families skew heavily winter/autumn; terpenic (coconut/salty/tropical) skews summer — directionally sane.
- **06_whitespace.py** → `outputs/06_territory_scores.csv` (15 families, every component column visible), `data/interim/06_family_share_by_year.parquet`. Used brand-demeaned approval/desire in the composite score (a modeling choice, not a CLAUDE.md fixed decision — flagged for review). Top 3 by score: cluster 11 (savory→gourmand-nutty), 7 (oud), 2 (balsamic). Cluster 4 (soapy/ubiquitous) is bottom by a wide margin, driven by its outsized supply share.
- **07_figures.py** → `outputs/figures/{a_note_map,b_launch_share_over_time,c_demand_vs_supply}.png`, 200dpi.
- **06_whitespace.py (revised)** → cluster names now top-8-notes-by-frequency (`outputs/cluster_names.csv`), not top-lift accord. Cluster 4 (backbone: musk/bergamot/sandalwood/vanilla/jasmine/amber/patchouli/cedar, 59% of launches) split out of the ranking with its own `backbone` flag; z-scores recomputed over the remaining 14 families only. Top territory changed from cluster 11 to cluster 7 once cluster 4 stopped distorting the z-score reference population.
- **06_whitespace.py (sentiment fix)** → sentiment component switched from raw net weight to `sentiment_polarity = (pos-neg)/(pos+neg)`; `sentiment_net_raw` kept for comparison. Confirmed the confound: cluster 6 (rose/musky) had the highest raw net weight of any family (106,456) purely from word-frequency volume, but a middling polarity (0.807) below cluster 11's (0.882, raw only 26,933). Added `confidence` tier (HIGH/MEDIUM/LOW from `n_products_weighted`, report-only). Re-ranking is substantial — new #1 is cluster 11 (caramel/almond/chocolate), not cluster 7 (oud) as before.
- **07_figures.py (touched, not requested standalone)** → while building the results notebook, found `b_launch_share_over_time.png` and `c_demand_vs_supply.png` were stale (predated the backbone-exclusion/sentiment-polarity fixes to `06_whitespace.py`) — `c`'s annotated top-3 literally contradicted the current ranking table. Re-ran 07 to refresh both from current checkpoints, and while doing so changed all three figures' cluster labels to pull from `outputs/cluster_names_final.csv` (previously `cluster_name` baked into each checkpoint, which is the long top-8-note slash-form) — the long names were also overflowing figure c's title once refreshed. Small, targeted edit only (label source + a `final_names` param threaded through); no other logic changed.
- **notebooks/results.ipynb** → assembled per spec: loads only saved `data/interim/*.parquet`/`outputs/*.csv`/PNGs, no recomputation. 62 cells (31 markdown/31 code), verified with a real restart-and-run-all via `nbclient` (not just visual inspection) — 0 errors, no code cell lacking a preceding markdown cell. Installed `nbformat`/`ipykernel`/`nbclient` into the venv for this (not in the original allowed-library list; needed to build/execute a notebook at all). One rounding bug caught and fixed: the section-10 per-family evidence-trace `Series.to_frame()` mixed strings and floats into an `object`-dtype column, which silently defeated `.round(3)` and printed full float precision — fixed by rounding numeric fields explicitly before building the Series.
- **01c_extract_similar.py** → `data/interim/01c_similar_pairs.parquet` (2,337,784 pairs: also_liked 1,645,055, reminds_me_of 692,729). Population-filtered: 951,939 (40.7%). 17,334/32,499 population products (53.3%) have >=1 confident (net_votes>=5) outgoing reminds_me_of edge. reminds_me_of is only 58.9% symmetric (A→B doesn't reliably imply B→A); also_liked is 2.9% symmetric (expected, it's algorithmic not mutual). Viable ground-truth set for recommender eval.
- **08_recommender.py** → 5 representations (note_svd, note_tfidf, family, accord, hybrid) × top-50 cosine neighbours via sklearn NearestNeighbors (no dense NxN matrix), `data/interim/08_neighbours_{rep}.parquet`. Eligible sets differ slightly: 32,429 have notes (70 population products have none), 32,495 have accords, hybrid (note_svd ∩ accord) = 32,428.
- **09_recommender_eval.py** → evaluated against 01c's confident reminds_me_of edges (17,334 evaluable queries) at k=5/10/20 vs 3 mandatory baselines (random, popularity, same-brand). **note_tfidf wins every one of 12 metrics**; hybrid is a consistent second. **family representation loses to the same-brand baseline on all 12 metrics** — its 15-d membership vector is too coarse to distinguish individual products, worth dropping or not using standalone for retrieval. random is near floor as expected; popularity and same-brand are both clearly beaten by note_svd/note_tfidf/accord/hybrid.
- **10_build_app.py** → `outputs/app/index.html` (6.49 MB, self-contained, no CDN/server/fetch). Top 3,000 products by relation.have, neighbours from note_tfidf (best rep per 09). Payload dedupes product metadata across all neighbour lists (23,142 distinct products referenced, stored once each) rather than embedding full records per neighbour occurrence — kept the JSON payload at 6.48 MB, under the ~8MB budget. Explainability (shared notes, family overlap) is computed client-side from each product's own notes/family already needed for display, not precomputed/duplicated per pair. Verified with jsdom (installed to /tmp for testing only, not a project dependency): fuzzy search, product select, same-brand filter, and neighbour drill-down all confirmed working end-to-end, zero JS errors.
- **03c_nonlinear_clustering.py** → evidence only, 03's k=15 KMeans is unchanged and untouched. Installed umap-learn/minisom/gensim/networkx. Findings: (1) hierarchical — average linkage wins cophenetic correlation (0.822) but its cuts at every k are **degenerate chaining** (488-456/492 notes in one mega-cluster, ARI vs KMeans@15 ~0.001-0.01); ward has much worse cophenetic correlation (0.273) but is the only linkage giving a balanced partition (largest cluster 34.6% at k=5) — caught and explicitly flagged rather than reporting the trivial "0.99 nesting purity" as a genuine finding, since it was really just "everything's in one blob." Saved `outputs/hierarchical_k5_superfamilies.csv` flagged degenerate. (2) UMAP(nn=15/30, min_dist=0)→HDBSCAN finds only 2 clusters either way, ARI~0.005 — confirms the earlier 03b HDBSCAN-finds-noise result even with the standard manifold-first pipeline. (3) skip-gram (gensim, sg=1, 50d) vs PPMI+SVD: ARI=0.32 — moderate agreement, expected per Levy & Goldberg 2014 (skip-gram implicitly factorises shifted PPMI). (4) 6x6 SOM: ARI=0.39 vs KMeans@15, u-matrix has no sharp ridges (range 0.31-1.0, patchy not bimodal) — continuity, not discrete islands. (5) Louvain on PPMI≥1.0 graph (12,990 edges): 16 communities, modularity 0.25 (below the 0.3 weak-structure line), ARI=0.22. All five methods independently point the same direction: real structure but no natural discrete boundaries at any k — consistent with 03b's conclusion that k=15 is a chosen discretisation, not a discovered one.
- **11_param_optimisation.py** → `outputs/11_param_search.csv` (48 configs), `outputs/figures/k_param_search.png`. Split 17,334 evaluable queries 60/20/20 by product (train=10,400/val=3,467/test=3,467), seed 42, before any tuning. Confirmed (by re-reading 09's source) that ground truth was already directional — symmetric scoring would have inflated every metric by ~0.001-0.003, shown numerically on the test split for contrast. Grid search on validation: note_tfidf gain over untuned default +0.0011 (negligible — use_idf=False even edges out the default slightly), note_svd +0.0060 (small; n_components=200 wins monotonically, PPMI shift=2 consistently hurts, shift=0/1 tie exactly by construction), hybrid alpha=0.4 +0.0123 (small but the largest of the three — hybrid remains the best single fixed representation on the test split at precision@10=0.0516, ahead of tuned note_tfidf 0.0430). All tuning gains are honestly small; none of it changes the qualitative story from 09 (learned representations still beat all three baselines by a wide margin; family still weak).
- **notebooks/results.ipynb (extended, sections 11-14)** → 96 cells (48 markdown/48 code), verified via nbclient restart-and-run-all, 0 errors, no code cell missing a preceding markdown. To keep "load saved outputs only" honest for the new sections (09/11/03c only ever *printed* their headline tables, never saved them), added minimal `to_csv` saves to those three scripts — `outputs/09_evaluation.csv`, `outputs/11_test_results.csv`, `outputs/11_selected_config.csv`, `outputs/03c_summary.csv` — and re-ran all three (numbers reproduced exactly, same as their original standalone runs). No other logic touched in any of them.
- **requirements.txt + ENVIRONMENT.md** → `pip freeze` (75 packages). Cross-checked the "added beyond allowed list" claim rather than trusting it: found `hdbscan==0.8.44` genuinely present but unused (dependency-graph debris from initial 03b exploration — install timestamp matches kneed's exactly; sklearn's built-in `HDBSCAN` was used everywhere, confirmed via grep — no `import hdbscan` in `src/`). Documented all 9 added packages with one-line justifications each; noted jsdom explicitly as Node-only test tooling, never in the venv.
- **Reproducibility check (fresh venv, full pipeline from raw data)** → 15 scripts re-run in a clean venv built from `requirements.txt` (corrected 2026-08-13 from an earlier "16" miscount in this entry — `ls src/*.py | wc -l` is 15; see the later reproducibility-check entry below), in an isolated directory (raw data symlinked, `cluster_names_final.csv` copied in as the one human-authored non-pipeline input). All 3 mandated diffs clean: `06_territory_scores.csv` identical to 3dp, `taxonomy_map.csv` ARI=1.000000 (492/492 notes, exact label equality too), `09_evaluation.csv` identical to 4dp. Extra diligence beyond the required scope found one real divergence: `note_normalisation.csv` row `calone`'s REVIEW annotation cites a different tied fuzzy-match example (`calypsone` vs `cascalone`, both similarity exactly 0.80) between runs — root-caused to Python's default per-process string-hash randomization affecting `set` iteration order in `02_normalise_notes.py`, cascading into which tied candidate `max()` picks to cite. Confirmed contained to that one cosmetic annotation string (the `canonical`/`action` columns and everything downstream matched exactly, since the actual merge logic unions all qualifying pairs regardless of order). Not fixed — reported for a decision (pin `PYTHONHASHSEED=0`, or add a deterministic tie-break to that one `max()` call).
- **03b_cluster_selection.py** → evidence-only diagnostics for k/algorithm, does not change 03's k=15/KMeans. Installed `kneed` (not in the original allowed-library list; used because explicitly requested). Findings: kneed's knee detector picks k=13 (close to 15, not a strong disagreement); silhouette/CH/DB are all fairly flat and low past k~8-10 (no dramatic elbow); bootstrap stability (20 resamples) is highest at k=8 (mean ARI 0.34) and monotonically declines to 0.22 by k=25, with k=15 at 0.26 ± 0.036 — moderate, not high, reproducibility; AMI vs. each note's highest-lift accord is flat (~0.16-0.17) across all tested k, actually *peaking* at k=15 (0.173) among the tested values; HDBSCAN found **zero** clusters (100% noise) at min_cluster_size 10 and 20, and only 2 clusters at min_cluster_size 5 — the note embedding has no strong density-based structure; GMM-full agrees moderately with KMeans@15 (ARI 0.50, expected since KMeans is GMM's spherical/equal-covariance limit), agglomerative-ward/cosine and spectral agree only weakly (ARI 0.20-0.34). Outputs: `outputs/figures/{d_cluster_selection,e_cluster_stability,f_dendrogram,g_ami_vs_k,h_gmm_bic}.png`, `outputs/cluster_algorithm_comparison.csv`.

## 2026-08-13, later session: hybrid reconciliation, two-tab app, cleanup, docs

- **Hybrid definition reconciliation (08/09/11)** → read both scripts' source rather than trusting the logs: `08_recommender.py` built "hybrid" as z-scored note_svd+accord concatenation; `11_param_optimisation.py` tuned alpha over a note_tfidf+accord score blend — genuinely different objects being compared as if interchangeable. Adopted the note_tfidf+accord score blend as canonical (it beat the old definition by a wide margin on every test-split metric: precision@10 0.0516 vs 0.0393, hit_rate@10 0.3518 vs 0.2788). Rebuilt `08_recommender.py`'s hybrid to that formula with a neutral untuned default (alpha=0.5, not the tuned 0.4) so the untuned-vs-tuned comparison in 11 stays meaningful instead of collapsing to a no-op. Re-ran 08→09→11 in order. New numbers: hybrid now wins all 12 metrics in `09_evaluation.csv` outright (precision@10=0.0519) even at the untuned default; 11's tuned-vs-untuned gain for hybrid dropped to −0.0002 (negligible/noise, both settings sit near the flat peak of the validation curve). Re-executed the notebook to pick up the new numbers.
- **04_demand.py (additive)** → added `data/interim/04_product_demand.parquet` (per-product approval/desire raw+demeaned signals; previously only the family-level aggregate was saved). Needed so `10_build_app.py`'s per-note reception stats could load an existing checkpoint instead of re-deriving the Bayesian-shrinkage/demeaning formula a third time (avoiding the same kind of definitional-drift risk the hybrid reconciliation just caught). `04_family_demand.parquet` numbers unchanged (verified byte-for-byte against the printed diagnostic table).
- **10_build_app.py + 10_app_template.html (rebuilt as a two-tab app)** → `outputs/app/index.html`, 10.26 MB (under the 12MB ceiling given for this rebuild; no cuts needed). **Tab 1, Note Explorer** (new): search any of 492 canonical notes; profile (family, prevalence, top-5 accords by lift, tier split), common (raw co-occurrence) vs. distinctive (PPMI lift) partners shown side by side with explicit framing copy, top-10 cosine-similar notes, reception (brand-demeaned approval/desire vs. population) with season/gender skew, family's territory-score row (confidence tier visible), up to 10 example products, and an interactive 2–5 note combination builder (exact occurrence count via client-side product-id set intersection — ships every note's population product-id list, ~2MB, specifically so a genuinely arbitrary combination can report a true zero rather than only ever hitting a precomputed list; pairwise PPMI lift restricted to a shipped top-50-per-note lookup, "below reporting threshold" outside it; family mix; suggested additions by mean lift). **Tab 2** unchanged in spirit (product similarity, now on the reconciled tuned hybrid, alpha=0.4) but rebuilt to compute its own neighbours directly (the tuned config only existed as validation/test-subset computations before) and to show shared accords (not just shared notes) in the explainability panel; footer names the exact representation/config from the payload rather than a hardcoded string. **Bug caught in testing, not shipped**: 19 population products have their `community_gender` vote group independently nulled (real per `01_clean.py`'s `has_votes` design — a group can be null while others are real) while my new per-note aggregation didn't account for that, producing `NaN` in the JSON for any note touching one of those 19 products — invalid JSON that would have broken `JSON.parse` in a real browser. Fixed by excluding null-vote products from that specific aggregation (not zero-filling, which would misrepresent "no data" as "no votes"), and added `allow_nan=False` to the build so this class of bug fails loudly at build time from now on. Verified with jsdom: tab switching, note search, a low-prevalence note edge case (`daisy`, n=30, the exact DROP_RARE floor — no crash across any panel), palette add/remove, and the explicit empty-combination case (found a genuine zero-overlap pair, confirmed the UI reads "0 — zero. That combination does not occur in the population." rather than erroring).
- **requirements.txt cleanup** → removed `hdbscan` (confirmed unused: no `import hdbscan` anywhere in `src/`, nothing depends on it per `pip show`/reverse dependency check, sklearn's built-in `HDBSCAN` is what's actually called in `03b`/`03c`). While investigating, also found and removed `pipdeptree` and a `build`/`installer`/`pyproject_hooks`/`tomli`/`tomli_w`/`truststore`/`nab-index`/`nab-python`/`nab-resolver` chain that turned out to be pip's own packaging tooling, pulled in as a side effect of a `pip` operation, not a project dependency. `requirements.txt` now 74 packages (was 75). Re-verified every project import (including `sklearn.cluster.HDBSCAN`) still works after the cleanup. `ENVIRONMENT.md` updated with a "Removed" section recording what and why.
- **Brand-demeaning decision — confirmed, not changed** → `06_whitespace.py`'s composite score has always used brand-demeaned approval/desire (raw values were confounded by brand-prestige halo); the code comment previously described this as "flagged for review," which was no longer accurate — it was never actually in question, just never formally written down as settled. Updated the comment to say so plainly, and added `METHODS_NOTES.md` as the durable record: composite always uses demeaned; raw is reported only where an absolute (non-relative) level is being described, never fed into the score. No computation changed — this was documentation only. `METHODS_NOTES.md` also records the hybrid-definition reconciliation decision for the same reason (a modelling choice outside CLAUDE.md's fixed decisions, now settled and written down).
- **README.md** (new, repo root) → written for someone who has never seen the project: one-paragraph summary, Python 3.12 + venv + `pip install -r requirements.txt`, exact raw-data path (`data/raw/perfumes.jsonl`, not included, no download link since none was ever given — told to obtain it from whoever supplied it), all 15 scripts in run order with one-line descriptions and rough runtimes (not 16 — counted `src/*.py` directly rather than assume the number was right), which files are human-authored (`cluster_names_final.csv`, the override dicts in `02_normalise_notes.py`) and why that matters for anyone re-running the pipeline, how to register the notebook's pinned kernel and open the app, an outputs guide (question → file), and the reproducibility note (what verified identical, the one cosmetic divergence found and fixed).

## 2026-08-13, later session: five verification/documentation fixes

- **Reproducibility re-check against the current pipeline state** → the earlier check (line above) had validated a state that no longer existed (pre-hybrid-reconciliation 08, no `04_product_demand.parquet`, 75-package `requirements.txt`, single-tab app). Repeated the full protocol — fresh venv from the current `requirements.txt`, isolated directory, raw data symlinked, `cluster_names_final.csv` copied in, all 15 scripts run in order from raw data — and diffed 6 files (up from 3, to cover everything that changed): `06_territory_scores.csv` (3dp, 0/255 numeric cells differ), `taxonomy_map.csv` (ARI=1.000000, 492/492 notes, exact label equality too), `09_evaluation.csv` (4dp, 0/104 cells differ), `11_test_results.csv` (4dp, 0/143 cells differ), `note_normalisation.csv` (byte-identical, MD5 match — confirms the earlier hash-seed tie-break fix has held), `app/index.html` (same size, 10,755,196 bytes, JSON payload parses in both, and in fact MD5-matched byte-for-byte too, though that's not guaranteed on every run since dict ordering isn't pinned). **No divergence found.** `README.md`'s Reproducibility section rewritten from this result rather than reused: dated (2026-08-13), scoped to the pipeline state it validates, all 6 files listed, and the earlier check's now-fixed cosmetic divergence kept as history rather than restated as current.
- **Script-count reconciliation** → direct count (`ls src/*.py | wc -l`) is 15; confirmed `src/10_app_template.html` is not a script (the HTML/CSS/JS template `10_build_app.py` fills in) and `src/__pycache__/` isn't source. README's "15 scripts" was already correct. The "16 scripts" in this file's earlier reproducibility-check entry (line 25) was a plain miscount — not a description of a genuinely different past state, since the very next entry in this file (line 35, same session) already said "not 16" — so it's now corrected to 15 in place, with a note explaining why, rather than left as a wrong number standing uncorrected next to a correct one.
- **Audit for superseded note_tfidf/hybrid claims** → grepped `notebooks/results.ipynb` (markdown cells), `README.md`, `METHODS_NOTES.md`, `ENVIRONMENT.md`, `src/07_figures.py`, `src/11_param_optimisation.py`, and the app footer/template for `note_tfidf`/`hybrid`/`wins`/`best`/`second`/`beats` before changing anything, per instruction. **Zero stale claims found** — the "note_tfidf wins all 12 / hybrid second" narrative from the pre-reconciliation run never made it into any hand-written prose outside this file's own historical log entry (line 19, correctly left untouched — it's accurate history from before the reconciliation). Nothing needed correcting.
- **Tuning-result reframing (hybrid's −0.0002 test-set gain)** → not previously misdescribed as a failure anywhere (the existing `11_param_optimisation.py` print already said "negligible", not "failed"), but nothing existing actually explained *why* a negative gain here is expected and fine, either. Added that explanation in three places: a new paragraph in `METHODS_NOTES.md`'s hybrid section (states the −0.0002 number plainly, frames it as a flat validation-curve peak, ties it to the other two representations' equally-small gains, and to why `08_recommender.py`'s default stays at alpha=0.5 rather than the tuned 0.4); a new markdown cell in the notebook right after the gain table (notebook re-executed via nbclient after the insert, 97 cells, 0 errors); and one added `print()` in `11_param_optimisation.py` after the existing verdict loop. Re-ran `11_param_optimisation.py` to confirm the new print statement — `git status` on `outputs/` was clean afterward, confirming this was a pure text change with zero effect on any number.
- Net effect of all five: no analytical computation changed anywhere (confirmed by the reproducibility re-check finding zero divergence, and by `outputs/` being untouched after the TASK 4 script re-run) — every fix in this session was verification, correction of a wrong number/count, or added/corrected prose.
